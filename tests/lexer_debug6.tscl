// Lexer debug - checking lexer structure
console.log("=== Lexer Debug 6 ===");

// Create lexer directly inline
let lex = createLexer("42");

// Check individual fields
console.log("lex.pos: " + lex.pos);
console.log("lex.line: " + lex.line);
console.log("lex.col: " + lex.col);
console.log("lex.source: " + lex.source);

// Check source length directly
let srcLen = lex.source.length;
console.log("lex.source.length: " + srcLen);

// Test charCodeAt directly on the source
let charCode = lex.source.charCodeAt(0);
console.log("lex.source.charCodeAt(0): " + charCode);

console.log("=== Done ===");
